#include<stdio.h>
int main()
         {
             int a;
             a=0;
             if(a>=50)//here the condition only applies to line before the upcoming terminator
             printf("pass");
             printf("bye");
             return 0;

         }